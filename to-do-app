<!DOCTYPE html>
<html lang="so">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Liiska Hawlaha</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #4cafef, #7b1fa2);
      color: white;
      text-align: center;
      padding: 20px;
    }
    h1 {
      font-size: 2.2em;
      margin-bottom: 10px;
    }
    .input-section {
      margin: 20px 0;
    }
    input {
      padding: 10px;
      width: 60%;
      border: none;
      border-radius: 5px;
      font-size: 1em;
    }
    button {
      padding: 10px 15px;
      border: none;
      background-color: #ff9800;
      color: white;
      border-radius: 5px;
      font-size: 1em;
      cursor: pointer;
      margin-left: 5px;
    }
    button:hover {
      background-color: #e68900;
    }
    ul {
      list-style: none;
      padding: 0;
    }
    li {
      background: rgba(255, 255, 255, 0.1);
      margin: 8px 0;
      padding: 10px;
      border-radius: 5px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 1.1em;
    }
    li.completed {
      text-decoration: line-through;
      color: #bbb;
    }
    .delete-btn {
      background-color: red;
      border: none;
      color: white;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
    }
    .delete-btn:hover {
      background-color: darkred;
    }
  </style>
</head>
<body>
  <h1>📋 Liiska Hawlaha Maalinlaha ah</h1>
  <div class="input-section">
    <input type="text" id="hawsha" placeholder="Qor hawsha aad rabto...">
    <button onclick="kuDarHawl()">Ku Dar</button>
  </div>
  <ul id="liiska"></ul>

  <script>
    function kuDarHawl() {
      const hawshaInput = document.getElementById("hawsha");
      const hawshaText = hawshaInput.value.trim();

      if (hawshaText === "") {
        alert("Fadlan qor hawl marka hore!");
        return;
      }

      const li = document.createElement("li");
      li.textContent = hawshaText;

      li.addEventListener("click", function() {
        li.classList.toggle("completed");
      });

      const deleteBtn = document.createElement("button");
      deleteBtn.textContent = "Tirtir";
      deleteBtn.classList.add("delete-btn");
      deleteBtn.onclick = function() {
        li.remove();
      };

      li.appendChild(deleteBtn);
      document.getElementById("liiska").appendChild(li);

      hawshaInput.value = "";
    }
  </script>
</body>
</html>